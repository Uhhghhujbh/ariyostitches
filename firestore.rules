rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DENY ALL PUBLIC ACCESS
    // All access must go through the backend (Vercel Functions) using the Service Role SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
