
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all read/write access from client SDKs
    // All operations must go through the backend API using Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
